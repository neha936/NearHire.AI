# ── Core (required) ─────────────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pydantic>=2.6
python-dotenv>=1.0

# ── HTML scraping (Priority 2 sources) ──────────────────────────
beautifulsoup4>=4.12
lxml>=5.2
aiohttp>=3.9

# ── Scheduling ──────────────────────────────────────────────────
APScheduler>=3.10

# ── Caching (optional Redis backend; in-memory fallback otherwise)
redis>=5.0

# ── Optional PostgreSQL persistence (off unless DATABASE_URL set) ─
SQLAlchemy>=2.0
asyncpg>=0.29

# ── Optional JS rendering for SPA sites (Wellfound etc.) ─────────
# Install the browser separately: `playwright install chromium`
playwright>=1.44

# ── Tests ───────────────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.23
