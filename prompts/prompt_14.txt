"Integrate AWS SES into my Node.js (MS1) backend to implement secure email OTP authentication. Replace the current login flow with a two-step login: (1) user enters email and password, backend validates credentials, generates a secure 6-digit OTP, stores a hashed OTP with a 5-minute expiry in PostgreSQL (or Redis if available), and sends it via AWS SES; (2) user enters the OTP, backend verifies it, invalidates it after successful use, issues the JWT, and logs the user in. Also implement OTP resend with a 60-second cooldown, rate limiting, proper error handling, environment-based AWS credentials, reusable emailService.js, clean controller/service architecture, secure validation, and update the React frontend to support the new email OTP verification flow with loading states, countdown timer, and user-friendly messages."


"Add a complete 'Forgot Password' feature to my NearHire project. Add a 'Forgot Password?' link on the React login page. When clicked, navigate to a Forgot Password page where the user enters their email. The Node.js backend should verify the email, generate a secure 6-digit OTP, hash it, store it with a 5-minute expiry, and send it using AWS SES. Create APIs for POST /api/auth/forgot-password, POST /api/auth/verify-reset-otp, and POST /api/auth/reset-password. After successful OTP verification, allow the user to set a new password, hash it with bcrypt, update the database, invalidate the OTP, and redirect back to the login page. Include resend OTP (60-second cooldown), rate limiting, validation, error handling, loading states, countdown timer, and a modern responsive UI that matches the existing NearHire design."


"Debug and fix my AWS SES OTP email integration in the Node.js backend. The frontend shows 'OTP sent successfully' but no email is received. Verify AWS credentials, SES region, verified sender identity, sandbox restrictions, IAM permissions, environment variables, emailService.js configuration, API flow, backend logs, and error handling. Ensure the backend logs the exact AWS SES response/error, only returns success after the email is actually sent, and fix any issues preventing OTP delivery. Also verify that the recipient email can receive emails in SES sandbox mode or guide the project to production-ready SES configuration."